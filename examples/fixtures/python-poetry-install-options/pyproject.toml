[project]
name = "python-poetry-install-options"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "pytest"
]

[build-system]
requires = ["poetry-core>=1.9.0"]
build-backend = "poetry.core.masonry.api"
